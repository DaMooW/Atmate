/**
 * 输入区（tech §4）。M0 占位；M1（T1.5）：发送/停止；M2 起：素材卡片 MaterialCard。
 */
export function Composer() {
  return (
    <div className="border-t border-border p-2">
      {/* TODO M1: MaterialCard(素材+图片) + 发送/停止 */}
    </div>
  );
}
