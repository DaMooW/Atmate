# Mission（使命书）

> 版本：v0.4 · 2026-09-13 · 状态：定稿（评审通过）
> v0.4 变更（评审定案，2026-09-13）：正式定名"在伴"，英文 wordmark "Atmate"（Q1 已决；未联网查重，上架前自查）；素材落点定案（默认当前会话·一键转新，见 roadmap T2.4）；M1 联调端点池定为 DeepSeek/智谱 GLM/Kimi/OpenRouter/硅基流动（明细见 roadmap M1 验收）；M6 候选池增"对照视图""一键直发"（附"无黑箱"张力注记）；界面中英切换升为承诺功能（原 M6 i18n 候选，NFR-6 相应收紧）。
> v0.3 变更：图像输入纳入必须范围（D-010）——新增 FR-2.6、FR-4.6、FR-5.5、场景 E、S-5；【必须】范围扩为 M1–M4；Out-of-scope 仅保留音频/视频。
> v0.2 变更：采纳"提示词层 + 上下文供给"路线——新增 FR-1.6（全局基础指令·信息完备性声明）、FR-2.5（上下文范围）、场景 D；Out-of-scope 更新（受限工具层不实现）。
> 定位：本文件定义产品"是什么、为谁做、做到什么程度"。技术实现见 [techniqueStack.md](techniqueStack.md)，阶段计划见 [roadmap.md](roadmap.md)。

## 0. 一句话定位

**在伴**（**Atmate**；前工作名 AITranslator，2026-09-13 定名）是一款 Chrome 侧边栏 AI 角色助手：在任意网页与 PDF 上用鼠标划取内容（含图片），配上一个自定义的 AI 角色（提示词预设），即可获得流式的 LLM 输出（翻译、解读、摘要、追问……），并对 token 用量与上下文占用一目了然。

## 1. 背景与问题

- 阅读/翻译/研读网页与 PDF 时，经常需要"把这段内容丢给 AI 处理"——目前在 ChatGPT 网页与原文之间反复切换、复制粘贴，体验割裂。
- 用户往往需要 AI 带着**特定身份**来处理内容（某个领域的翻译官、论文审读人、代码审查员），通用聊天窗口每次都要重新交代提示词。
- 使用自备 API Key 的用户，缺少对**成本与上下文**的可视化：一次对话花了多少 token、上下文还剩多少，多数工具不展示或事后才展示。
- 市面上的划词工具大多绑定自家服务或单一模型；需要一个**协议中立（OpenAI 兼容端点即可）、本地存储、无遥测**的轻量替代品。

## 2. 目标用户与核心场景

**目标用户**：自备 LLM API 的开发者与重度阅读者（DeepSeek / 智谱 GLM / Kimi / OpenRouter / OpenAI / 本地 Ollama 等任一兼容端点的用户）。

### 场景 A：划词翻译
用户在阅读英文技术文档，划选一段话 → 点浮动按钮发送到侧边栏 → 选择"翻译官"角色 → 在补充框输入"术语按前端领域、保留代码块" → 流式输出译文。读下一段时，基于同一会话继续追问，无需重复交代要求。

### 场景 B：论文/长文研读
在 PDF 中划选一段方法描述 → 发送到侧边栏 → "学术解说员"角色 → 输出通俗解释；后续划选的内容进入同一会话，AI 拥有前文语境，可被追问"这和上一段说的方法矛盾吗"。

### 场景 C：随手处理 + 量控
用户切换到较贵/较便宜的模型配置，观察状态条上"当前上下文 x / 上限 y token"和会话累计 token，决定何时开新会话；上下文接近上限时获得预警而不是静默截断。

### 场景 D：信息缺口闭环
用户划选一段内容发送，AI 先给出基于现有信息的回答，并在回复中明确列出"缺失信息清单"（如：需要上游依赖版本、原文上文）；用户在同一会话继续划选/粘贴补足，多轮闭环后 AI 自行整合。获取信息靠"AI 声明缺口 + 用户供给"，不靠工具抓取。

### 场景 E：图表与扫描件（D-010）
用户读一篇带方法图的论文：右键发送图表（或供给档位自动附带正文配图），AI 解读其结构与文字；遇到扫描版 PDF（无文字层）时，"附当前页为图片"成为唯一输入通道。图片与文本走同一素材卡片与 token 治理（FR-5），所见即所发。

## 3. 功能需求（FR）

标注：【必须】M1–M4 交付；【加分】M5 起在 [roadmap.md](roadmap.md) 排期；【暂缓】明确接受长期不做。

### FR-1 角色系统【必须】
- FR-1.1 用户可创建角色：名称 + 系统提示词（system prompt），可选图标/描述。
- FR-1.2 角色可保存为预设列表，支持编辑、删除、复制。
- FR-1.3 内置示例角色若干（翻译官、摘要助手、代码审查员、学术解说员），首次启动时自动生成；内置角色与用户自建角色同等可编辑、可删除；提供"恢复默认角色"入口（仅补回不存在的默认角色，不覆盖已有）。
- FR-1.4 角色绑定于会话：新建会话时选择角色；会话内消息构成 system + 多轮历史。
- FR-1.5 【加分】会话中途切换角色（历史保留，system 更换）；角色导入/导出（JSON）。
- FR-1.6 全局基础指令【必须】：设置开关（默认开）。开启时，每个请求的 system 在角色提示词之后追加一段由扩展统一维护的固定指令，至少包含三条：① **信息完备性判断**——先评估现有素材是否足以完成任务；② 信息不足但可部分作答时，先给出基于现有信息的回答，再明确列出"**缺失信息清单**"，请用户补足；③ 不得编造缺失的信息或来源。该指令不要求用户写进角色提示词。

### FR-2 内容输入【必须】
- FR-2.1 网页划词：获取选中文本及来源（页面标题 + URL），发送到侧边栏。
- FR-2.2 补充输入：用户可在发送前编辑选中文本、追加补充说明（如"术语按 XX 领域"）。
- FR-2.3 手动输入：不划词，直接在侧边栏输入框提问。
- FR-2.4 PDF 划词：在扩展自建的 PDF 查看页内划词，链路与网页一致（见 NFR-3 与技术文档 §9）。
- FR-2.5 上下文范围【必须，M2 起落地】：发送划词内容时可选供给档位：① 仅选区（默认）；② 选区 ± 相邻段落；③ 附整页正文（自动提取失败时降级并明确标注）；PDF 查看页内另有"附全文"档。任一档位的组装结果一律先经 token 预览与限额治理（FR-5）才发送，且素材卡片上可见最终将发送的内容——无黑箱。
- FR-2.6 图片输入【必须，M4 落地，D-010】：以配置开启视觉能力（FR-4.6）为前提，素材可携带图片：
  - FR-2.6.1 网页右键图片 →"发送此图片到 AI 侧边栏"，携带来源页与 alt 文本；
  - FR-2.6.2 "±相邻段落 / 附整页正文"供给档自动附带范围内图片（解析懒加载/srcset 的真实源），单次发送图片总数 ≤4 张，素材卡片以缩略图呈现、逐张可移除；
  - FR-2.6.3 PDF 查看页提供"附当前页为图片"（扫描件与图表页的主通道）；框选页面区域【加分】；
  - FR-2.6.4 输入区粘贴图片【加分】；
  - FR-2.6.5 归一化：长边 ≤1568px、重编码为 JPEG 后以 data URL 内联发送；图片字节与文本一致直发所配端点、无第三方（NFR-2），不要求端点回源抓图。

### FR-3 AI 输出【必须】
- FR-3.1 多轮对话，流式渲染（SSE），支持 Markdown（含代码块、表格、LaTeX 暂缓）。
- FR-3.2 输出进行中可"停止生成"。
- FR-3.3 【加分】失败重试、单条"重新生成"；思维链（reasoning）内容折叠展示。

### FR-4 API 配置【必须】
- FR-4.1 支持多 API 配置，字段见 FR-4.2；配置可编辑、删除、即时切换，发送时使用当前激活配置。
- FR-4.2 每套配置包含：名称、Base URL、API Key、模型 ID、**思维强度**、上下文上限（token，可自动预填）、请求附加参数（JSON，加分）、温度（加分）。
- FR-4.3 思维强度：档位（关闭/轻/中/深），按配置映射为对应请求字段（`reasoning_effort`、`budget_tokens` 或自定义 JSON 模板），随请求发送。
- FR-4.4 API Key 在 UI 中默认脱敏显示，不写入任何日志，不同步到任何远端。
- FR-4.5 连接测试：保存前可"发送一条最小请求"验证配置。
- FR-4.6 视觉能力开关【必须，M4】：配置项"该模型支持图片输入"，默认否；素材含图而未开启时禁止发送并给出引导（开启开关或移除图片）。

### FR-5 Token 计量【必须】
- FR-5.1 状态条常驻显示：**当前上下文 ≈x / 上限 y**（带进度条与阈值变色：<70% 常态、≥70% 黄、≥90% 红）。
- FR-5.2 会话累计 token：当前会话所有请求的花费总和（优先取 API 返回 usage；无返回时按估算并在 UI 标注"≈"+"估"角标）。
- FR-5.3 上下文达到上限时：禁止发送并给出引导（M5 起提供一键"压缩历史"，支持剥离早期图片），不允许静默截断。
- FR-5.4 【加分】单条消息 hover 显示该轮 prompt/completion 分解。
- FR-5.5 图片 token 计量【必须，M4】：估算模式下按 ≈(宽×高)/750 计入上下文占用与素材 token 预览并标"≈"（与 FR-5.2 规则一致）；精确模式由 usage 覆盖；超限治理（FR-5.3）对图片与文本同权适用。

### FR-6 会话管理【必须】
- FR-6.1 多会话：列表、重命名、删除；持久化于本地（chrome.storage.local），重开浏览器不丢失。
- FR-6.2 会话记录所用角色与各消息，token 统计按会话归属。

### FR-7 划词入口【必须】
- FR-7.1 划选文字后，选区附近显示浮动按钮（Shadow DOM 隔离，避免与站点样式互相污染）；点击 → 打开侧边栏并携带选区。
- FR-7.2 右键菜单项："发送选中内容到 AI 侧边栏"。
- FR-7.3 【加分】快捷键唤起；对输入框/密码框等场景自动抑制浮层。

## 4. 非功能需求（NFR）

- **NFR-1 性能**：侧栏交互（打开、切换会话、发送）UI 不阻塞；流式渲染增量追加而非全量重渲染；打开面板到可输入 < 300ms（本地环境）。
- **NFR-2 安全与隐私**：API Key 仅存 chrome.storage.local；无遥测、无账号体系、不经任何第三方中转——请求（含图片字节，FR-2.6.5）直连用户配置的 Base URL（声明 host 权限是唯一例外，见技术文档 §3）。
- **NFR-3 兼容性**：Chrome 稳定版 ≥ 114（Side Panel API）；Manifest V3；任何"OpenAI 兼容 `/v1/chat/completions`"端点。PDF 划词依赖扩展自建查看页（Chrome 内置 PDF 查看器不可注入脚本，属平台限制，见技术文档 §9）。
- **NFR-4 健壮性**：SSE 中断/网络错误给出可读错误与重试入口；配置项有校验（URL 格式、必填、数值范围）。
- **NFR-5 可维护性**：分层架构（ui / core / infra），TS strict，ESLint + Prettier，核心模块（LLM 客户端、SSE 解析、token 估算、存储 schema）有单元测试。
- **NFR-6 语言**：UI 默认中文，设置内一键切换英文，切换即时生效（v0.4 起 i18n 为承诺功能，非预留）；文案全量字典化（含错误提示、内置角色示例），时间/数字格式随所选语言本地化；用户自建角色内容不译。

## 5. 明确不做（Out of Scope）

- ❌ Tool calling / 代码执行 / 主动获取网页或文件的 Agent 工作流（2026-09-13 评审结论：信息缺口改由"基础指令声明 + 用户补足"（FR-1.6）与"上下文供给"（FR-2.5）解决；受限工具能力待真实使用反馈后重评，见技术文档 ADR-007）
- ❌ 音频、视频输入（图片输入已纳入必须范围，见 FR-2.6 与决策日志 D-010；音频/视频在网页与 PDF 阅读场景罕见、载体成本不成比例——出现真实场景时再议）
- ❌ 本地运行模型、RAG / 知识库
- ❌ 云端账号、跨设备同步、协作
- ❌ 整页翻译、批量抓取页面
- ❌ 自建 AI 服务／代理转发（永远直连用户端点）

## 6. 成功标准

- **S-1 闭环**：在 Chrome 114+ 上加载扩展后，3 分钟内完成一次"打开任意网页 → 划选文字 → 选'翻译官' → 补充要求 → 收到流式输出"且全程无复制粘贴。
- **S-2 可信**：token 状态条与 API 返回 usage 的偏差在"精确模式"下为 0，"估算模式"下 ±15% 以内并明确标注"≈"。
- **S-3 透明**：抓包可验证所有请求仅出现两方——扩展与用户配置的 Base URL。
- **S-4 可续**：新功能全部走 spec 流程（见 roadmap §1），每个里程碑有可勾选的验收清单。
- **S-5 读懂图像**：配置 vision 模型后，发送网页图表与扫描版 PDF 页，AI 能正确转述图中文字与结构。

## 7. 术语表

| 术语 | 含义 |
|---|---|
| 角色（Role） | 一组 system prompt 预设，绑定于会话 |
| 会话（Session） | 一次多轮对话及其元数据（角色、消息、统计） |
| 素材（Material） | 划词发送来的选中文本 + 来源信息，发送前可编辑 |
| API 配置（ApiConfig） | Base URL/Key/模型 ID/思维强度等的一套完整参数 |
| 思维强度（Thinking Level） | 用户侧档位（关/轻/中/深），按配置映射到具体请求字段 |
| 上下文上限（Context Limit） | 模型上下文窗口 token 数，配置项，带常见模型预填 |
| 精确/估算模式 | token 数取自 API usage 字段 / 由扩展本地估算 |
| 基础指令（Base Directive） | 扩展统一追加在角色 system 末尾的固定指令段（含信息完备性条款，FR-1.6） |
| 上下文供给（Context Supply） | 发送素材的信息供给档位：仅选区 / ±相邻段落 / 附整页正文 / 附 PDF 全文（FR-2.5） |
| 视觉模型（Vision） | 配置中"该模型支持图片输入"的开关与其要求的模型能力（FR-4.6） |
