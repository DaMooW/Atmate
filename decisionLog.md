# Decision Log（决策日志）

> 版本：v0.2 · 2026-09-13 · 状态：使用中
> v0.2 变更：追加 D-010（图像输入纳入必须范围）；因插入新 M4，原 M4（精度与体验）与 M5（方向池）顺延为 M5/M6，历史条目中的里程碑引用已同步。
> 定位：完整记录本项目"问题 / 技术 / 产品抉择 / tradeoff"的产生与解决过程。产品与技术决策**全部**收录（超集）；纯架构结论以 [techniqueStack.md](techniqueStack.md) §12 ADR 为准，本文件存其对话背景与出处索引。需求范围见 [mission.md](mission.md)，实现约束见 [techniqueStack.md](techniqueStack.md)，排期见 [roadmap.md](roadmap.md)。

## 记录约定

- **编号**：D-XXX，三位递增，永不复用；条目只追加，不删除、不合并。
- **改判**：不改写原条目——新立记录，旧条目状态改为"已被取代（→ D-XXX）"。每个决策必须携带"重评条款"（什么条件下复议），无则写"无"。
- **时间**：按日记录，不虚构钟点；决策生效以需求方确认回复的时刻为准。
- **决断人**：标注"用户确认（多选/自由答复）"或"Claude 建议·待评审"。
- 字段固定：时间 · 标题 · 描述 · 候选方案与 tradeoff · 最终采取 · 解决 · 重评条款；"相关"备选。

## 索引

| ID | 时间 | 标题 | 类别 | 状态 |
|---|---|---|---|---|
| D-001 | 2026-09-12 | 交互形态：Chrome 侧边栏 | 产品 | 定案 |
| D-002 | 2026-09-12 | LLM 协议：OpenAI 兼容单协议 | 技术 · tradeoff | 定案（子设计待评审） |
| D-003 | 2026-09-12 | 对话模式：多轮会话 | 产品 | 定案 |
| D-004 | 2026-09-12 | 技术栈：TS+React+Vite，落地 WXT | 技术 | 定案（WXT 待评审） |
| D-005 | 2026-09-12 | 流式请求位置：扩展页面发起 | 技术 · tradeoff | 暂定 |
| D-006 | 2026-09-12 | PDF：自建 viewer 接管 | 技术（平台限制）· 产品 | 暂定（Q2 未决） |
| D-007 | 2026-09-12 | Token 计量：精确/估算双模 | 技术 · tradeoff | 暂定（Q4 挂钩） |
| D-008 | 2026-09-12 | 权限策略：`<all_urls>` | tradeoff | 暂定（Q5 挂钩） |
| D-009 | 2026-09-13 | 信息缺口：指令层+上下文供给，不做工具 | 产品 · tradeoff | 定案（含 L1 重评条款） |
| D-010 | 2026-09-13 | 图像输入：纳入图片，维持音频/视频排除 | 产品 · tradeoff | 定案 |
| D-011 | 2026-09-14 | storage 回流忽略"自身写入回声" | 技术 · 缺陷修复 | 定案 |
| D-012 | 2026-09-15 | 会话标题未自动生成的修复 | 技术 · 缺陷修复 | 定案 |
| D-013 | 2026-09-15 | 思维强度强制置灰 + 空对话默认角色 | 产品 · 技术 | 定案 |

---

## D-001 · 交互形态：Chrome 侧边栏

**时间**：2026-09-12（夜，初始 spec 会话） ｜ **类别**：产品 ｜ **状态**：定案 ｜ **决断人**：用户确认（多选）

**描述**：扩展主交互形态决定 UI 架构走向：需承载多轮对话、流式长输出阅读、划词入口，且兼容 MV3 与长期可维护性。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 |
|---|---|---|
| 侧边栏（Chrome 114+ Side Panel API）+ 划词入口 | 常驻对话区；AI 类扩展主流形态 | 实现复杂度中 |
| 工具栏 Popup | 实现最简 | 小窗口不适合长文与持续使用；划词靠右键中转 |
| 页面内悬浮面板 | 体验最沉浸 | 最复杂：站点样式冲突 / CSP / 隔离 |

**最终采取**：侧边栏 + 划词入口（多选中带"推荐"标签，用户原样确认）。

**解决**：mission FR-7（划词入口）、FR-3（输出）；tech §2（Side Panel 为 React SPA 宿主）、§4（视图层级）；M0 起 sidepanel 入口；最低 Chrome 版本锁定 114（tech §3）。

**重评条款**：当目标用户群受 114+ 份额约束时，讨论 popup 降级形态（未列入计划）。

**相关**：D-004

---

## D-002 · LLM 协议：OpenAI 兼容单协议

**时间**：2026-09-12（夜） ｜ **类别**：技术 · tradeoff ｜ **状态**：主决策定案（思维强度映射子设计待评审）｜ **决断人**：主决策用户确认（多选）；子设计 Claude 建议

**描述**：扩展须支持用户自配的任意 LLM 端点。目标用户群横跨至少十余家兼容端点（DeepSeek / 智谱 / Kimi / OpenRouter / Ollama / OpenAI…），各家"OpenAI 兼容"实为方言（usage、reasoning 字段差异）；"思维强度"参数各厂商命名不一。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| OpenAI 兼容 `/v1/chat/completions` | 一套格式通吃绝大多数端点（含 vision 的 content parts，D-010），填 BaseURL 即切换 | 方言差异需探测+降级 | 采纳 |
| Anthropic 原生 | 面向官方 Claude 端点最优 | 需第二套完整实现 | 落选，留 M6 候选（D-010 顺延） |
| 双协议 | 覆盖最全 | 请求/SSE 两套 ×2，初期开发量近倍 | 落选 |

**最终采取**：OpenAI 兼容单协议。子设计（暂定）：思维强度以三结构映射——`reasoning_effort` 值表 / `budget_tokens`（thinking 字段）/ `custom` JSON 模板（`{{level}}` 插值），用户侧四档（关/轻/中/深）与配置侧解耦。

**解决**：mission FR-4.3；tech §6（请求构造、思维链字段别名 `reasoning_content`/`reasoning`、collectUsage 探测降级）；ADR-003；roadmap M1 T1.2/T1.4。

**重评条款**：当实际所需官方 Claude 端点与 OpenAI 兼容面出现真实能力差（如缓存、interleaved thinking），评估 Anthropic 协议第二实现（M6 候选池）。

---

## D-003 · 对话模式：多轮会话

**时间**：2026-09-12（夜） ｜ **类别**：产品 ｜ **状态**：定案 ｜ **决断人**：用户确认（多选）

**描述**："划选→输出"可做成单次触发，也可以做成持续性会话。决定数据模型与 token 治理的粒度。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| 多轮对话 | 上下文累计、可追问、支持会话列表与分会话治理 | 实现中（持久化、会话管理） | 采纳 |
| 单次触发 | 实现最简 | 不可追问、AI 无前文可交叉推导，场景 B"深读"不成立 | 落选 |

**最终采取**：多轮对话。

**解决**：mission 场景 B / D（"声明缺口+用户补足"闭环的唯一载体）；FR-3.1、FR-6；tech §5（Session/ChatMessage）；roadmap M1 交付会话骨架。

**重评条款**：无。

**相关**：D-009 的整个机制以本条为前提。

---

## D-004 · 技术栈：TS + React + Vite，落地 WXT

**时间**：2026-09-12（夜） ｜ **类别**：技术 ｜ **状态**：框架家族定案；WXT 子选择待评审 ｜ **决断人**：框架家族用户确认（多选）；WXT 为 Claude 建议，评审摘要已告知

**描述**：选定语言、UI 框架与扩展构建链。约束：MV3 多入口（sidepanel/content/background/viewer/options）、manifest 长期维护、开发 HMR。

**候选方案与 tradeoff**：

| 决策点 | 候选 | 结论与理由 |
|---|---|---|
| 语言 | TypeScript strict / JS | TS——存储 schema 必须类型守护 |
| UI 框架 | React 18 / Svelte / 无框架 | React——生态与参考组件最多；无框架违背可续性 |
| 构建链 | 手写 manifest+esbuild / CRXJS / **WXT** | WXT——MV3+多入口一等公民、内含 Vite、2026 主流；CRXJS 维护放缓；手写为长期负债（ADR-001），但列为 WXT 失守时的迁移路径 |

**最终采取**：TS + React + Vite（多选确认），构建链落地为 WXT。状态侧 zustand、样式侧 Tailwind v4 为 Claude 建议·待评审（tech §1 #4/#5）。

**解决**：tech §1 #1–#5；roadmap M0 T0.1–T0.3；ADR-001。

**重评条款**：WXT 停止维护或构建链安全问题阻断发版 → 切"Vite + 手写 manifest 生成"路径（WXT 本质是该路径的自动化，迁移成本可控）。

---

## D-005 · 流式请求位置：扩展页面发起，不走 background

**时间**：2026-09-12（夜） ｜ **类别**：技术 · tradeoff ｜ **状态**：暂定 ｜ **决断人**：Claude 建议（v0.1 起草时自主决定，评审摘要已告知）

**描述**：MV3 background 为 Service Worker，~30s（有事件活动时最长 5 分钟）后杀进程，长流式连接极易中途夭折。需定 fetch 发起方。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| 扩展页面（sidepanel/viewer）直接 fetch | 页面生命周期随用户，无需保活 | 面板关闭即中断；需跨上下文消息路由 | 采纳 |
| Background 发起 | 面板关闭不中断 | keep-alive 舞蹈复杂且脆；SW 被杀"停止"状态丢失 | 落选 |

**最终采取**：扩展页面发起。"面板关闭即中断"语义正确（用户不在看输出），accept（ADR-002）。

**解决**：tech §1 #8、§8；对话请求仅从 sidepanel 触发。D-010 后该约束扩展为"content script 也不取图片字节"——采集只上报 URL，取回与归一化统一在 side panel（tech §6）。

**重评条款**：若未来出现"后台生成 / 关面板继续"需求 → 推翻本条，迁 background+keep-alive。

---

## D-006 · PDF：自建 viewer 接管，非注入原生查看器

**时间**：2026-09-12（夜） ｜ **类别**：技术（平台限制）· 产品 ｜ **状态**：暂定（接管默认行为 Q2 未决）｜ **决断人**：Claude 建议（v0.1 评审摘要已告知"不接受则重写 M3"，暂无异议）

**描述**：Chromium 的 PDF 查看器是浏览器内部页面（`chrome://` 系），扩展无法注入 content script——PDF 内获取原文是平台事实，无技术绕过。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| 自建 viewer 页（pdf.js）接管 | 唯一可靠路径，接管后**复用与网页完全一致的划词链路** | 新入口（pdf.js 数百 KB——动态 import 缓解）、导航接管要用户 opt-in，数人日 | 采纳 |
| 声明 PDF 不支持 | 最省力 | 场景 B（论文深读）硬性失败 | 落选 |
| 假装部分支持 | 低成本 | 右键菜单在 PDF 页行为不保证；违背"诚实优于假装支持"原则 | 拒绝 |

**最终采取**：自建 viewer + 用户开关接管，默认行为"每次询问"（Q2 暂取"默认询问"，待用户决定）；file:// 场景给开"允许访问文件网址"引导；未接管时段 UI 诚实标注不可用并说明原因。

**解决**：mission FR-2.4、NFR-3；tech §9；roadmap M3；ADR-005。D-010 追加：viewer 工具栏提供"附当前页为图片"（扫描件唯一输入通道）。

**重评条款**：若 Chromium 后续开放 PDF 页注入，或用户反馈接管导航困扰大于价值 → 重议 Q2 默认值乃至转"声明不支持"。

---

## D-007 · Token 计量：精确/估算双模

**时间**：2026-09-12（夜） ｜ **类别**：技术 · tradeoff ｜ **状态**：暂定（Q4 挂钩）｜ **决断人**：Claude 建议（v0.1 起草时决定，评审摘要已告知）

**描述**：FR-5 需真实示出"上下文 x/上限 y"与"会话累计 token"。但相当多兼容端点流式不回 usage（或需 `stream_options.include_usage` 非标参数）；本地 BPE 库数百 KB 且各家分词器不同。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| ① 仅精确（usage） | 最真 | 无 usage 端点缺整个功能 | 拒绝单用 |
| ② 仅估算 | 最稳 | 误判成本风险，违 S-2"可信" | 拒绝单用 |
| ③ 本地 BPE | 较准 | +数百 KB；各家分词差异→精度增益打折 | 暂缓（Q4） |
| ④ tradeoff | 尽真且全覆盖 | 需双实现+模式标注 | 采纳 |

**最终采取**：④ 双模：usage 优先；缺时字符系数估算（ceil/3.2，中英混合±15%）兜底，UI 强制"≈"+口径 hover；≥70% 黄 / ≥90% 红 / 达限禁发；M5 校准系数。D-010 后估算面扩展至图片（≈(宽×高)/750）。

**解决**：tech §7；roadmap M1 T1.6/T1.8（估算）→ M5 T5.1（精确）/T5.5（校准）（里程碑因 D-010 顺延）。

**重评条款**：Q4——M5 实测后校准误差仍 >±15%，评估引入 BPE 库（图片公式一并校准）。

---

## D-008 · 权限策略：`<all_urls>` 而非可选白名单

**时间**：2026-09-12（夜） ｜ **类别**：tradeoff ｜ **状态**：暂定（Q5 挂钩，触发点在上架前）｜ **决断人**：Claude 建议（同上）

**描述**：用户可配 BaseURL 任意且随时可改；扩展页面需跨域 fetch（端点）+ 取 PDF 字节。需定 host 权限策略。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| 静态 `<all_urls>` | 零配置成本 | 安装提示"读取所有网站数据"；商店审核压力与用户信任成本 | 采纳（当前个人/开发用途） |
| `optional_host_permissions` + 填 BaseURL 时动态申请 | 商店友好 | 每改端点需重新授权、重启级提示；多端点高频切换不可用 | 落选（上架时重议） |

**最终采取**：`<all_urls>` + 应用描述与 NFR-2 如实声明"仅两方：扩展↔用户端点，无遥测、无中转"。D-010 后用途清单 +1：取回页面图片做归一化（tech §3）。

**解决**：tech §3；mission NFR-2/S-3；roadmap 开放问题 Q5。

**重评条款**：启动 Chrome Web Store 上架流程时重议 optional 化。

---

## D-009 · 信息缺口：指令层 + 上下文供给，不做工具层

**时间**：2026-09-13（凌晨 spec 会话） ｜ **类别**：产品 · tradeoff ｜ **状态**：定案（附 L1 重评条款）｜ **决断人**：用户确认（自由答复，含超出建议方案的新增条款）

**描述**：用户提出——筛选内容告知 AI 的方式可能"信息不全"，或"AI 想要另外的信息"，是否应让扩展具备 agent 能力（打开文件/网页、search 获取信息）？

分析：需求横跨两类，成本截然不同——"信息不全"（AI 缺上下文）绝大多数靠数据供给层即可解决，不需要工具；"AI 想要另外的信息"（需外部世界）才需要"获取能力"，而能力深度是一个谱系。分层评估：

| 层 | 能力 | 成本 | 结论（当时） |
|---|---|---|---|
| 零层 | AI 在回复中声明缺口，用户下轮粘贴/划词补足 | 极低；多轮会话（D-003）天然支持 | 基础 |
| L0 | 上下文供给：选区±段落/整页正文（readability）/PDF 全文 | 极低；无新权限，护栏复用 FR-5 | 并入 M2/M3（T2.6 / T3.6） |
| L1 | 受限工具（read_source_page / open_url + 确认卡，单次 1–2 轮） | 中：tool calling 协议兼容面、prompt 注入面、确认交互、结果计量 | 暂禁 |
| L2 | 自主 agent（自由浏览+search） | 高：外部引擎依赖、可观测 UI、成本治理；且偏离"轻量阅读助手"定位 | out of scope |

**最终采取**：经用户自由答复确认为两层机制（其条款超出我的建议方案）：

1. **上下文供给（L0）**：发送素材时可选"仅选区 / ±相邻段落 / 附整页正文 / 附 PDF 全文"，采集结果经素材卡片透出+先过 token 预览（无黑箱）；"整页"失败降级时显著标注，不默认降级。
2. **基础指令（新增，用户主动补充）**——扩展在每次请求的 system 末尾自动追加一段由扩展统一维护的指令，含信息完备性判断条款：先判断信息是否足以完成任务；存在缺口但可部分作答时，先给基于现有信息的回答，再明确列出"缺失信息清单"，请用户补足；不得编造缺失信息或来源。全局开关默认开启，无需用户手写进每条角色提示词。
3. 工具层（L1/L2）不做；L1 保留重评条款。

**解决**：落地于——
- mission：FR-1.6、FR-2.5、场景 D（新）、术语表两条、Out-of-scope 更新；
- tech：§6（system 拼装顺序：角色在前、基础指令在后）、§8.1（`DIRECTIVE_V1` 版本化常量与设置开关）、§8.2（供给档位采集表）、风险表两项、ADR-007；
- roadmap：M1 +T1.9、M2 +T2.6、M3 +T3.6；变更记录 v0.2 行。

**重评条款**：若实测反馈证明"AI 声明 + 用户补足"不敷使用（反馈"等待补足回合数过多/因缺信息答错"频次可感），评估受限工具 L1（`read_source_page`/`open_url` + 展示完整 URL 的确认卡）——已入 M6 候选池，重评时须完整对话注入面与成本治理再定；L2 维持 out of scope。

---

## D-010 · 图像输入：纳入图片，维持音频/视频排除

**时间**：2026-09-13（决策日志建立同会话；用户对 v0.2 的 Out-of-Scope 提出异议） ｜ **类别**：产品 · tradeoff ｜ **状态**：定案 ｜ **决断人**：需求由用户提出（自由消息）；分层方案 Claude 建议·随 spec v0.3 一并评审

**描述**：v0.2 将"多模态输入（图片/音频）"整体列入 Out-of-Scope。用户指出：**网页与 PDF 含图片是常态**（方法图、插图、截图、扫描件），尤其扫描版 PDF 无文字层——排除图片等于对真实内容形态"失明"，与"阅读网页与 PDF"的产品定位直接冲突。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| 维持排除（v0.2 现状） | 零成本：消息纯文本 | 图表/扫描件/带图教程全盲；扫描 PDF 完全不可用 | 推翻 |
| 图片输入（vision 端点） | 覆盖常态内容形态；扫描 PDF 唯一通道 | 消息模型 parts 化、二进制存储、token 计量、须 vision 模型 | 采纳 |
| OCR 代替 | 无需 vision 模型 | 新引擎依赖；丢失图表结构与排版语义 | 拒绝 |
| 音频/视频输入 | — | 网页/PDF 阅读场景罕见；载体与流量成本不成比例 | 维持排除 |

**最终采取**：图片输入纳入【必须】（新 M4）；音频/视频维持 Out of Scope。关键约束：
① 视觉能力按配置勾选（默认否），素材含图未开启时禁发并引导（FR-4.6）；
② 图片一律在扩展侧归一（长边 ≤1568px → JPEG q0.85 → data URL 内联），不要求端点回源抓图，与 NFR-2"仅两方"一致（ADR-008）；
③ 入口三+一：右键图片 / 供给档附图（≤4 张/次，素材卡片逐张可移除）/ PDF"附当前页" / 粘贴（加分）；
④ 图片 token 估算 ≈(宽×高)/750 标"≈"，治理并入 FR-5；
⑤ 二进制分置 IndexedDB，不入 storage.local。

**解决**：因插入新 M4，原 M4（精度与体验）与 M5（方向池）顺延为 M5/M6，四文档内所有里程碑引用已同步。落点——
- mission：FR-2.6、FR-4.6、FR-5.5、场景 E、S-5、§3 标注扩为 M1–M4、Out-of-scope 改为仅音频/视频；
- tech：§3（+`unlimitedStorage`）、§5（ContentPart/StoredPart、`ApiConfig.vision`、IndexedDB 图片分置、schema v1→v2）、§6（图片消息构造与归一化管线）、§7（图片 token 公式）、§8.2（附图档）、§9（viewer 页图入口）、风险表 +3、ADR-008；
- roadmap：新 M4（T4.1–T4.8，4–6 人日），原 M4/M5 重编号（T4.x→T5.x），Q4/Q7 引用同步；
- decisionLog：本条；D-002/D-007/D-009 的里程碑引用同步。

**重评条款**：图表文字清晰度不足 → 评估 PNG 保留/提高长边上限/提高 JPEG 质量；音频出现真实用户场景 → 单独评审是否纳入。

---

## D-011 · storage 回流忽略"自身写入回声"（M1 验收缺陷修复）

**时间**：2026-09-14 ｜ **类别**：技术 · 缺陷修复 ｜ **状态**：定案 ｜ **决断人**：用户确认（要求"开 spec、修竞态、补测试"）

**描述**：M1 人工验收（真实 DeepSeek 端点）发现流式输出丢内容：服务端完整输出 793 字，应用落库 550 字，中间缺块、结尾截断，代码块因此未渲染成 `<pre>`。定位到 `infra/storage/store.ts`：`chrome.storage.onChanged` 回流把本上下文自己写入产生的回声当成外部变更，仅做引用比较就无条件套用；而 `storage.local.set` 是异步的，回声到达时存储里往往已是更晚的值（探针实测：200 次快速写入，199 次回声到达即为过期值）。把旧快照写回内存后，后续流式增量叠加在旧内容上，中间那段内容永久丢失。

**候选方案与 tradeoff**：

| 方案 | 收益 | 成本/风险 | 结论 |
|---|---|---|---|
| 忽略自身回声（写入登记稳定指纹，命中即出队忽略） | 根治状态倒退；外部变更仍照常回流；不改 schema | 每次写入/回声各做一次稳定序列化 | 采纳 |
| 时间窗内一律忽略回声 | 实现最简 | 会吞掉其他上下文的真实变更，且依赖魔法时长 | 拒绝 |
| storage 载荷加写入版本号 | 顺序判断最严格 | 需改 schema v1，代价大于收益 | 拒绝 |
| 流式期间不写盘、结束再落盘 | 顺带解决写放大 | 崩溃即丢整段输出；不解决回声竞态本身 | 记入 M5 候选（写盘节流） |

**最终采取**：写入前登记值的**稳定指纹**（对象键递归排序后序列化 + FNV-1a 32 位，FIFO 上限 512）；onChanged 命中即视为自身回声并出队忽略，未命中才按外部变更回流。

**关键坑**：storage 会把写入值重新序列化，**回声里的对象键序与写入时不同**（变为字母序）。首版指纹直接用 `JSON.stringify`，实测 325 次写入 / 325 次回声 / 命中 0 次，缺陷照旧。改为键序无关的稳定序列化后：312 次写入 312 次命中、0 未命中，落库内容与服务端流逐字一致（634 字同指纹，含代码围栏与 usage）。

**解决**：

- 代码：`infra/storage/store.ts`（`stableStringify` / `rememberLocalWrite` / `consumeLocalEcho` + 回流抑制）；
- 测试：`tests/infra/storage.test.ts` +6 例（滞后回声、积压回声、乱序回声、键序重排、外部变更仍回流、混合批量）；去掉修复后其中 4 例转红，去掉键序规范化后键序用例转红；
- 文档：tech §5、§12 ADR-010、roadmap §10、M1 validation §2.2；
- spec：`specs/20260914-m1-fix-storage-echo/`。

**重评条款**：出现多上下文并发写同一键（M2/M3 引入 background 业务状态）时，重新评估"最后写入者获胜 + 回声抑制"是否足够；届时考虑写入版本号（schema v2）或按消息粒度合并。

---

## D-012 · 会话标题未自动生成的修复（M1 验收缺陷）

**时间**：2026-09-15 ｜ **类别**：技术 · 缺陷修复 ｜ **状态**：定案 ｜ **决断人**：用户确认（"D-1 直接改"）

**描述**：M1 验收 §2.8 发现会话标题没有按"首条用户消息前 20 字"生成——所有经对话流程创建的会话标题都是"新对话"（首个为"新会话"）。定位到 `ChatView.updateSessionMessages`：首次发送用 `text.slice(0,20)` 写对了标题，但流式增量回调里该函数用渲染闭包中的 `sessions` 判断"会话是否存在"；流式期间闭包不包含刚创建的新会话，于是每次增量都走"新建会话"分支，用 `title ?? '新对话'` 重建会话对象，把首次写入的标题覆盖。

**最终采取**：会话列表一律以 `useStorageStore.getState().sessions` 为准（不再读渲染闭包）；标题统一走 `generateSessionTitle`（取首条用户消息、超 20 字加 `...`、空则"新会话"）；新建分支显式传入 `session.roleId` 与首次标题。测试：`core/session.generateSessionTitle` 已有 L1 覆盖；`builtinRoles` 相关由 D-013 一并补测；标题端到端行为在 Chrome 真机复验。

**解决**：`components/chat/ChatView.tsx`；M1 validation §2.8、§4。

**重评条款**：无。

---

## D-013 · 思维强度强制置灰 + 空对话默认角色（M1 验收修订）

**时间**：2026-09-15 ｜ **类别**：产品 · 技术 ｜ **状态**：定案 ｜ **决断人**：用户确认

**描述**：M1 验收暴露两个交互口径问题，用户裁定如下：

1. **D-2 思维强度**：选择不支持思维链的模型（能力表 `thinkingType === 'none'`）时，思维强度开关必须**强制置灰**，不允许手动再开启（原实现是"自动关闭 + 提示，但可手动重新勾选"）。
2. **D-3 空对话默认角色**：从空对话直接发送消息时，使用一个默认角色，其 System Prompt 即"在伴 Atmate"的产品介绍设定；因此新增第五个默认角色。

**候选方案与 tradeoff**：

| 项 | 方案 | 结论 |
|---|---|---|
| D-2 | A. 仅 `disabled` 置灰；B. 置灰并删除配置项 | 采纳 A：checkbox `disabled`，`checked` 强制 false，提示文案改"思维强度已禁用" |
| D-3 | A. 新增默认角色并用固定 ID 选中；B. 复用 `roles[0]` 约定 | 采纳 A：新增 `prompts/roles/atmate.md` + `builtinRoles.DEFAULT_ROLE_ID='builtin-atmate'`；空对话用 `roles.find(id===DEFAULT_ROLE_ID) ?? roles[0]`，不依赖数组顺序 |

**最终采取**：

- `components/settings/ApiConfigForm.tsx`：`thinkingDisabled = cap.thinkingType === 'none'`，checkbox 置灰且强制取消勾选；
- `prompts/roles/atmate.md`（新增）：在伴的产品介绍设定（划词 / 角色 / token 可见 / 仅两方 / 表达风格）；
- `core/builtinRoles.ts`：`BUILTIN_ROLES` 首位新增 `builtin-atmate`，导出 `DEFAULT_ROLE_ID`；
- `components/chat/ChatView.tsx`：`ensureSession` 优先 `DEFAULT_ROLE_ID`；
- 测试：`builtinRoles` 增补为 5 角色、默认角色与 prompt 关键词断言；`storage` seed 断言同步为 5。

**解决**：上述代码与 `tests/core/builtinRoles.test.ts`、`tests/infra/storage.test.ts`；M1 validation §2.6、§2.8、§4。

**重评条款**：若后续出现多个"默认/系统"角色需求，再考虑给 `Role` 增加 `isDefault` 标志或独立"系统角色"槽位。

---

## 维护说明

- 每个里程碑的 spec 评审中产生的新抉择、与旧结论的冲突，一律以新记录追加，并同步修改其"解决"字段的最新落点；三份 spec 文档与本日志由 roadmap §1 流程保持联动。
