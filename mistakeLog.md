# Mistake Log（智能体失误记录）

> 版本：v0.1 · 2026-09-13 · 状态：使用中
> 定位：记录**智能体（AI 协作方）**在这一项目里犯的失误——做错了什么、造成什么影响、根因是什么、以后怎么避免。
> 与 [decisionLog.md](decisionLog.md) 互补并分工：那份记"我们选了什么"（人机共同的产品/技术决策），这份记"我搞砸了什么"（单方可避免的操作事故）。
> 记录约定：编号 M-XXX，三位递增，只追加不删除；字段固定：时间 · 失误 · 影响 · 根因 · 处理 · 预防规则。"预防规则"必须可执行，不许写成口号。

## 索引

| ID | 时间 | 失误 | 严重度 | 状态 |
|---|---|---|---|---|
| M-001 | 2026-09-13 | 验证完静默停掉 dev 服务器，导致用户浏览器 localhost 打不开 | 中 | 已纠正 |
| M-002 | 2026-09-13 | 改 `outDir` 时让正在运行的 dev 服务器热重启失败崩溃，预览再次中断 | 中 | 已纠正 |

---

## M-001 · 停掉 dev 服务器导致 localhost 连接被拒

**时间**：2026-09-13 ｜ **严重度**：中（无数据损失，但打断用户且需额外一轮沟通） ｜ **状态**：已纠正 ｜ **记录人**：智能体自记

**失误**：M0 验收期间，我为了核对启动是否正常而运行了 `pnpm dev`；核验完成后，我把它当成"临时进程"结束掉了。但当时用户正通过浏览器预览 `http://localhost:3000/entrypoints/sidepanel/index.html`——服务器一停，用户看到的就是"**localhost 拒绝了我们的连接请求**"，只能回头问我怎么回事。

**影响**：

- 用户侧无谓中断一次，且最初无法判断是环境问题还是自己操作有误；
- 多消耗一轮对话用于排查一个本可避免的问题；
- 险些造成误判：看起来像"页面坏了"，实际是"服务被我关了"。

**根因**：

1. **环境所有权判断错误**：我把 dev 服务器视为"我的验证耗材"，忽略了它同时是用户正在使用的预览环境。
2. **状态变化未交代**：停止这类长驻服务时，我没有告知用户"它已经停了"，用户只能通过报错自己发现。
3. **验证方式选择了共享资源**：用与用户同一个实例/端口做验证，验证的收尾动作直接影响了用户。

**处理**：重新启动 dev 服务器并保持运行；同时把两件容易踩的坑告知用户——dev 服务器根路径一律 404，正确地址是 `http://localhost:3000/entrypoints/sidepanel/index.html`。

**预防规则**：

1. 凡是我启动的**长驻进程**（dev 服务器、监听端口的服务、后台任务），处置只有两种：要么**保持运行**并在收尾时明确告知用户；要么在**停止前**先确认没有人在用它。
2. 停止用户可能正在使用的服务前，必须显式说明"我要停掉 X，原因是……"，不做静默停止。
3. 需要"起一个服务来验证"时，优先用**独立端口 / 独立实例 / 独立配置目录**，把验证与用户环境隔离（本次验收 Chrome 的做法即为此列：临时配置 + 独立调试端口，用完关掉临时实例，不动用户环境）。

---

## M-002 · 改构建配置时把用户正在运行的预览服务器弄崩

**时间**：2026-09-13 ｜ **严重度**：中（预览中断约 2 分钟，无数据损失） ｜ **状态**：已纠正 ｜ **记录人**：智能体自记

**失误**：为把产物目录从隐藏的 `.output/` 换成 `dist/`（D10），我直接改了 `wxt.config.ts`。当时 dev 服务器正在运行，WXT 检测到配置变更后自动热重启，但重启时新旧配置混用，拼出了非法路径 `../../dist/chrome-mv3-dev/background.js`，构建立即失败、进程退出——用户的 localhost 预览又断了。这次是我收尾主动检查才发现的；如果没查，就会原样重演 M-001。

**影响**：用户预览中断（`curl` 返回 000），需要冷启动 dev 服务器才恢复；多消耗一轮排查。无数据损失。

**根因**：

1. **把"改构建配置"当成普通改代码**：改 `wxt.config.ts` 会让长驻的 dev 服务器重启，而重启本身有失败风险——这一步没有被我识别为"会影响用户环境的高风险操作"。
2. **又一次没有在动手前确认用户在使用什么**：与 M-001 同源，说明上次的结论没有落到这个场景上。

**处理**：干净地冷启动 `pnpm dev`（停掉旧进程后全新启动，而非继续热重启）→ 正常产出 `dist/chrome-mv3-dev`，预览恢复 200；同时确认生产构建 `pnpm build` → `dist/chrome-mv3` 正常。

**预防规则**：

1. 修改 `wxt.config.ts`、`package.json`、构建脚本等**会影响长驻进程的配置**前，先声明"这会让 dev 服务器重启"，改完后**必须验证重启成功**（看构建日志 + 预览可访问），不能只看代码本身没报错。
2. 热重启失败时，**停掉旧进程冷启动一次**来排除新旧配置混用；不要用反复热重启来试探。
3. 收尾时对用户可能正在使用的服务做一次健康检查（本例即请求一次预览地址、确认 200），确认无误再结束回合。
